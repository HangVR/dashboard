import { ProductFilePickerAdapter } from './product-file-picker-adapter';

describe('ProductFilePickerAdapter', () => {
  it('should create an instance', () => {
    expect(new ProductFilePickerAdapter()).toBeTruthy();
  });
});
